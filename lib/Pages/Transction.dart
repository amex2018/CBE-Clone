import 'package:flutter/material.dart';

class Transcation extends StatelessWidget {
  const Transcation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [const Text('Transcation Screen')],
    );
  }
}
