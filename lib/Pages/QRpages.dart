import 'package:flutter/material.dart';

class QRpages extends StatelessWidget {
  const QRpages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [const Text('QRpages Screen')],
    );
  }
}
